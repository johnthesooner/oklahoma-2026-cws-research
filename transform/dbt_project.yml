name: 'oklahoma_athletics'
version: '1.0.0'
config-version: 2
profile: 'oklahoma_athletics'

model-paths: ["models"]
test-paths: ["tests"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  oklahoma_athletics:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
