# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Jupyter
.ipynb_checkpoints/

# Note: build_manifest.json and charts/*.png ARE committed on purpose
# (deterministic generated assets, so reviewers can see them on GitHub).

# Football module: cached raw wikitext downloads (re-fetched by ingest_wikipedia.py)
# Cached source downloads (re-fetched on demand by the ingest/build scripts).
# NOTE: gitignore has no inline comments -- a trailing comment on the pattern line
# silently stops it matching, which committed 109 cached files once.
# football/sources/snapshots/ is deliberately NOT ignored: it preserves a dead upstream.
football/sources/raw/
.venv/
